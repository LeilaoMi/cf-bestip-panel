# 运维说明

本文档只保留在仓库中，不在公开页面展示。

## 日常维护

- 公开页面只保留地址提取、筛选、检测、导出和本地配置。
- 仓库说明、验收清单、发布记录只放在 `README.md` 与 `docs/`。
- 不要恢复 `functions/` 目录。
- 不要恢复运行时 `/api/*` 依赖。

## 缓存处理

如部署后页面不更新：

1. 访问 `?v=v4r15r2`；
2. 注销旧 Service Worker；
3. 清理站点数据；
4. 重新打开页面。
