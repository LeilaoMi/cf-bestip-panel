# Cloudflare Pages Functions 管理密钥。生产环境必须设置。
ADMIN_TOKEN=change-me-to-a-long-random-secret

# 本地服务监听地址。默认 127.0.0.1；需要局域网访问时改为 0.0.0.0。
HOST=127.0.0.1
PORT=8788

# 数据构建并发与单源最大响应体大小。
BUILD_CONCURRENCY=8
BUILD_MAX_SOURCE_BYTES=1048576
